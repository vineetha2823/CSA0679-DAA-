#include <stdio.h>
	int fact(int x)
{
	if(x==1)
	return 1;
	else
	return (x*fact(x-1));
}
void main()
{
	int fact(int);
	int x,y;
	printf("Enter a number:");
	scanf("%d",&x);
	y=fact(x);
	printf("Factorial of number is %d",y);
}
